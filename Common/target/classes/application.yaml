spring:
  datasource:
    url: jdbc:mysql://localhost:3306/exam
    username: root
    password: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
      dialect: org.hibernate.dialect.MySQL8Dialect
      format_sql: true
  application:
    name: Bean-Application
#  cloud:
#    consul:
#      host: localhost
#      port: 8500
#      discovery:
#        prefer-ip-address: true
#        instance-id: ${spring.application.name}:${random.value}
server:
  port: 8002




